# Vite development server port
VITE_PORT=8081

# Backend API port (for local development proxy only)
VITE_BACKEND_PORT=3000

# Backend API URL (for production deployment)
# Leave empty for development (uses Vite proxy)
# In production (Vercel), set to your full backend URL
# Example: VITE_BACKEND_URL=https://deploydriverall.onrender.com
VITE_BACKEND_URL=
